<UserControl x:Class="DeepNestSharp.Ui.UserControls.PartPlacementUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DeepNestSharp.Ui.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <GridView>
      <GridViewColumn Header="Source" Width="100" DisplayMemberBinding="{Binding Path=Source, Mode=OneWay}"/>
      <GridViewColumn Header="Id" Width="50" DisplayMemberBinding="{Binding Path=Id, Mode=OneWay}" />
    </GridView>
    <!--<StackPanel Orientation="Horizontal">
    <Label>Source</Label>
    <TextBlock Text="{Binding Source, Mode=OneWay}" />
    <Label>Id</Label>
    <TextBlock Text="{Binding Id, Mode=OneWay}" />
    </StackPanel>-->
  
</UserControl>
